package net.pkhapps.idispatch.core.auth.server.domain;

import org.jetbrains.annotations.NotNull;

public class Device extends Account {

    public Device(@NotNull AccountId id) {
        super(id);
    }
}
